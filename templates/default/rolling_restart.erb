#!/bin/bash

function die {
  echo "ERROR: $1"
  exit 1
}
instances=(<%= @instances.join(' ') %>)
for i in ${instances[@]}
  do ssh deploy@$i restart_app || die 'The app running on $i failed to restart'
done
