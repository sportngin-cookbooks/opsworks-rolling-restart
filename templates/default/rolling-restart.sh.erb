#!/bin/bash

function die {
  echo "ERROR: $1"
  exit 1
}
instances=(<%= @ip_addresses.join(' ') %>)
for i in ${instances[@]}
  do sudo -u <%= @user %> ssh <%= @user %>@$i <%= @app_restart_command %> || die "The app running on $i failed to restart"
done
